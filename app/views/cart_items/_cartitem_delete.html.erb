<%= form_with model: item, method: :delete, local:false, class:"delete_form" do |f| %>
  <%= f.hidden_field :product_id, value: item.product_id %>
  <%= f.submit "削除", class: "delete_btn" %>
<% end %>